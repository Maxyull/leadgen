httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
pytest>=8.0

# Optionnel : verification MX des domaines (commande `verifier`).
# Sans lui, `verifier` s'arrete proprement en le signalant.
dnspython>=2.6
